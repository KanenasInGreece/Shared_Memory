.env
*.env
# Credential-file backups and temp copies live BESIDE the real .env:
# bootstrap_tokens.sh mktemps "${ENV_FILE}.XXXXXX", and ad-hoc .env.bak-*
# copies have been observed inside a live checkout. Neither `.env` nor
# `*.env` matches those. `.env.example` is tracked and must keep shipping.
.env.*
!.env.example
.venv/
__pycache__/
*.pyc
__pycache__/
*.pyc
*.pyo
.DS_Store
*.log
memory/
MEMORY.md
shared-memory-skill/*.skill
CLAUDE.md
GEMINI.md
Local_Documentation/
research/
.claude/
.playwright-mcp/
HANDOFF.md
