# AGENT.md

**→ Read [`AGENTS.md`](AGENTS.md) — the canonical agent file for this repository.**

It serves both missions an AI coding agent is given here:

- **Operate the framework** (Part 1) — interview-driven first install (writes `shared-memory/.env` from the template), start/stop/status, token issuance, upgrade, backup.
- **Develop in this repository** (Part 2) — commands, architecture, key invariants, configuration.

This file exists only so agents that look for `AGENT.md` find their way; the two files previously carried duplicate guidance, which drifted. All content now lives in `AGENTS.md`.
