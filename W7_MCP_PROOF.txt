W7 — PHASE 4: LOCAL PROOF THAT THE PINNED MCP SPAWN COMMAND WORKS
(RULING 4 — "the MCP spawn surface is locally provable, which the
recommendation wrongly denied.")

WHAT THIS PROVES: the exact command form now shipped in mcp/mcp.json and
mcp/README.md -- `uv run --no-project --with-requirements
requirements-mcp.lock python mcp/vector-skill.py` -- actually spawns
mcp/vector-skill.py, completes a real MCP stdio initialize handshake, and
registers exactly the tool set tests/test_vector_skill.py's
test_the_mcp_server_registers_exactly_these_tools already pins (11 tools).
This is the live-spawn half of the proof the brief calls for; the in-process
half is the existing pinned test, which the build's exit-gate run also
executes.

METHOD: fastmcp's own `Client` + `StdioTransport` (both ship inside
requirements-mcp.lock, so no extra dependency was reached for) spawn the
server as a real child process using the literal pinned argv, complete the
MCP handshake over its stdio, and call `list_tools()` -- a genuine client
round-trip, not a source-code inspection. No credential file was read or
referenced (fact:1499): VECTOR_SKILL_ENV and SECURE_ENV_FILE were both
explicitly set empty in the spawned process's environment so it never
attempts to load a real .env, and no AGENT_TOKEN was set or needed --
enumerating the tool surface does not require authentication, only calling a
tool would.

Command run (from the worktree root):
  timeout 90 uv run --no-project --with-requirements requirements-mcp.lock \
    python /tmp/.../mcp_spawn_proof.py

That harness script itself spawns, as a SUBPROCESS, exactly:
  uv run --no-project --with-requirements requirements-mcp.lock python mcp/vector-skill.py
(cwd = the worktree root -- the same relative-path resolution the documented
mcp.json/mcp/README.md forms rely on.)

FULL TRANSCRIPT (verbatim):
----------------------------------------------------------------------
╭──────────────────────────────────────────────────────────────────────────────╮
│                                                                              │
│                         ▄▀▀ ▄▀█ █▀▀ ▀█▀ █▀▄▀█ █▀▀ █▀█                        │
│                         █▀  █▀█ ▄▄█  █  █ ▀ █ █▄▄ █▀▀                        │
│                                                                              │
│                                FastMCP 4.0.3                                 │
│                            https://gofastmcp.com                             │
│                                                                              │
│                🖥  Server:      Local_RAG_Orchestrator, 4.0.3                 │
│                🚀 Deploy free: https://horizon.prefect.io                    │
│                                                                              │
╰──────────────────────────────────────────────────────────────────────────────╯

[09/10/26 16:39:54] INFO     Starting MCP server                transport.py:242
                             'Local_RAG_Orchestrator' with
                             transport 'stdio'
Spawning: uv run --no-project --with-requirements requirements-mcp.lock python mcp/vector-skill.py
cwd: /home/xenofon/claude-labs/projects/shared-memory-GitHub/.claude/worktrees/agent-ada48a5ebd14a6e9b
Connection established (MCP initialize handshake completed).
Registered tools (11): ['archive_reasoning_trace', 'check_memory_health', 'graph_query', 'hybrid_search_and_rerank', 'memory_telemetry', 'record_lineage', 'review_hold', 'save_artifact', 'save_decision', 'save_retrospective', 'supersede']

Tool count: 11
MATCHES the pinned set in tests/test_vector_skill.py::_MCP_TOOLS (11 tools). PASS
----------------------------------------------------------------------

RESULT: PASS. The pinned command spawns cleanly, completes a real MCP
handshake, and the live-registered tool set is byte-for-byte the pinned set
in tests/test_vector_skill.py (`hybrid_search_and_rerank`, `save_artifact`,
`archive_reasoning_trace`, `save_decision`, `save_retrospective`,
`supersede`, `review_hold`, `check_memory_health`, `memory_telemetry`,
`record_lineage`, `graph_query` -- 11 tools), which mcp/README.md's own count
must also state (Group 1: mcp.json, mcp/README.md's count, both SKILL.md MCP
parity paragraphs, system-prompt.md -- all already covered by existing
tests in the suite, re-verified by this same exit-gate run).

NOTE — a residual concern outside this cycle's fix scope, recorded here
rather than acted on unilaterally: the pinned command's `--with-requirements
requirements-mcp.lock` argument is a RELATIVE path, exactly like the
gateway's own documented pinned form. It resolves correctly only when the
spawning process's cwd is the repo root (proven above). Neither mcp.json nor
mcp/README.md's worked example states a `cwd` for the MCP host to set, and
some MCP hosts do not default a spawned stdio server's cwd to the config
file's own directory. This mirrors an existing, unaddressed assumption in
the gateway's own documented line and is not new to this cycle's fix -- named
here as a finding, not fixed, since it was not in the brief's stated scope.
