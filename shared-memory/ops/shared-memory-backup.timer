[Unit]
Description=Scheduled Shared Memory backup (Postgres + Neo4j)

[Timer]
# Daily at 03:30 local; Persistent catches up a missed run after downtime.
OnCalendar=*-*-* 03:30:00
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
